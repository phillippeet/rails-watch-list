
  <p style="color: green"><%= notice %></p>

  <%= render @list %>

  <div class="container">
    <%= link_to "Edit this list", edit_list_path(@list), class: "button" %>

    <%= link_to "Back to lists", lists_path, class: "button solid-button" %>

    <%= button_to "Delete this list", @list, method: :delete, class: "button solid-button" %>
  </div>
</div>
